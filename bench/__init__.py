"""Benchmark / optimization tooling for the Connito miner.

This package is created cooperatively by multiple parallel units. Keep
the body of this file minimal so concurrent edits from sibling units
do not conflict — public APIs should live in their own submodules and
be lazily imported by callers (or imported explicitly from the submodule
path).
"""
